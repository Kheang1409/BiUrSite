apiVersion: v1
kind: Service
metadata:
  name: biursite-redis
  labels:
    app: biursite-redis
spec:
  type: ClusterIP
  selector:
    app: biursite-redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
